import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appcontify.starter',
  appName: 'mobile-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
